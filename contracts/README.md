# API Contracts

`openapi/kalm-api.v1.json` is the committed Milestone 0 API contract snapshot.

Generate it with `deploy\scripts\openapi.cmd generate` and validate it with
`deploy\scripts\openapi.cmd check` on Windows.
